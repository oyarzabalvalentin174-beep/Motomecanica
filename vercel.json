{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && if [ -d taller ]; then cd taller && npm install; fi",
  "buildCommand": "if [ -d taller ]; then cd taller && npm run build; else npm run build; fi"
}
